# Review comment template

How to render a `code-review` result as a PR/MR comment. **Presentation only** — the
wording of findings, their severity, and the verdict come from `code-review` and must
not be altered here.

## Structure

```markdown
## AI-assisted Code Review

**Status:** Changes requested  
**Reviewed commit:** `def456`  
**Base:** `dev`

### Blocking

1. **Possible null dereference**  
   `src/PaymentService.cs:84`

   The lookup result can be null before its `Id` property is accessed.

   **Recommended change:** Validate the result using the repository-standard not-found behavior.

### Warnings

1. **Missing test for the rejected-payment path**  
   `tests/PaymentServiceTests.cs`

   The new rejection branch has no coverage.

   **Recommended change:** Add a case asserting the rejection result.

### Automated checks

- `dotnet build` — Passed
- `dotnet test` — Passed

### Limitations

- Integration tests were not run because the required database was unavailable.

---

This review was generated by an AI assistant and should be validated by a human reviewer.

<!-- ai-review-state:
{
  "reviewedHeadSha": "def456",
  "reviewVersion": 1
}
-->
```

## Field mapping

| Comment element | Source |
| --- | --- |
| **Status** | `verdict`, rendered per the table below |
| **Reviewed commit** | `headSha` — the SHA actually inspected, never a branch name |
| **Base** | `baseBranch` |
| Sections | `findings` grouped by `severity`, keeping `code-review`'s order |
| Each entry | `title`, then `file:line`, then `message` + `impact`, then `suggestion` |
| **Automated checks** | `checks[]` — `name` and `status`, verbatim |
| **Limitations** | `limitations[]` |

Verdict rendering:

| `verdict` | Status line |
| --- | --- |
| `changes_required` | `Changes requested` |
| `pass_with_warnings` | `Approved with comments — human approval still required` |
| `pass` | `No blocking issues found — human approval still required` |
| `unable_to_complete` | `Review incomplete` |

Section headings by severity: `blocking` → `### Blocking` · `warning` → `### Warnings` ·
`suggestion` → `### Suggestions`.

## Rules

- **Omit empty sections.** No "### Warnings — none".
- Include a `rule` citation inline when the finding has one (e.g. "violates
  `AGENTS.md#layering`").
- Line references use `` `path:line` `` — the path as `code-review` reported it.
- Never add praise, a generic summary, or nits `code-review` did not raise.
- Never soften or omit a failed check. A `failed` check appears as `Failed`.
- On `unable_to_complete`, post the Limitations section and no findings section, and say
  plainly that the review did not complete.
- The trailing AI-assistance disclaimer is required on every posted comment.
- Two trailing spaces produce the line breaks in the header block — keep them.

## State marker

The HTML comment at the end is machine-readable state for incremental re-review. It is
invisible in rendered Markdown on all three providers.

```html
<!-- ai-review-state:
{
  "reviewedHeadSha": "def456",
  "reviewVersion": 1
}
-->
```

- `reviewedHeadSha` — the full head SHA reviewed. Must equal the **Reviewed commit** in
  the body.
- `reviewVersion` — starts at `1`, incremented on each re-review of the same PR.

On re-review, find the prior comment by this marker, take its `reviewedHeadSha` as the
previous point, and update the marker to the new head SHA. If the marker is missing or
unparseable, treat the PR as never reviewed and run a full review.

## Incremental re-review comment

When `reviewMode` is `incremental`, keep the same structure and add a range line plus a
resolved note:

```markdown
## AI-assisted Code Review (update 2)

**Status:** Changes requested  
**Reviewed commit:** `ghi789`  
**Previously reviewed:** `def456`  
**Base:** `dev`

_Reviewing commits `def456..ghi789`. Findings resolved since the previous review are not repeated._

### Blocking

#### Still unresolved

1. ...

#### Newly introduced

1. ...
```

Separate still-unresolved from newly-introduced findings. Name resolved findings briefly
("CR-001 null dereference — resolved") rather than restating them in full.

## Inline comments

Only when the file and line are reliable, the issue is concrete, the provider supports
inline placement safely, and it materially aids comprehension. Body format:

```markdown
**Possible null dereference**

The lookup result can be null before its `Id` property is accessed.

**Recommended change:** Validate the result using the repository-standard not-found behavior.

_AI-assisted review._
```

A finding posted inline should still be listed in the general comment so the reviewer
sees the complete picture in one place. Prefer one general comment over many inline ones.
